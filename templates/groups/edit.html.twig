{% extends 'base.html.twig' %}

{% block title %}Edit Tasks{% endblock %}

{% block body %}
    {{ include('groups/form.html.twig') }}
<hr>
    <h3>Add user to group</h3>
    <div class="card" style="width: 18rem;">
        <ul class="list-group list-group-flush">
            {% for user in users %}
                <li class="list-group-item"><a class="btn" href="{{ path('add_user', {'id': user.id, 'name': group.name}) }}">add</a> {{ user.username }}</li>
            {% else %}
                <li class="list-group-item">no records found</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
