    {{ form_start(form) }}
    <div class="form-group">
        {{ form_label(form.name) }}
        {{ form_widget(form.name, {'attr': {'class': 'form-control col-4'}}) }}
   </div>
   <div class="form-group">
    {{ form_label(form.id_user) }}
    {{ form_widget(form.id_user, {'attr': {'class': 'form-control col-4'}}) }}
</div>
<button class="form-control col-4 btn btn-primary" >{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
